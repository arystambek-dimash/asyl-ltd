# Известные открытые вопросы

Пункты, оставшиеся открытыми после технического аудита 06.09 (исходный отчёт —
`docs/audit/2026-09-06-remediation.md` в истории git). Перед работой над пунктом
проверьте, не закрыт ли он уже.

| Пункт | Что осталось |
|---|---|
| A16 | Диагностика данных зерна на проде, затем CHECK/unique-ограничения и правила редактирования ёмкости и резервов силосов; создание singleton `GrainSettings` не защищено. |
| A17 | Пагинация растущих списков со стабильным курсором и бюджет тяжёлых ответов. Глобальная пагинация без одновременной правки потребителей сломает API. |
| A18 | Решить, описывает ли счёт при смешанной оплате весь заказ или только часть по счёту. Ограничение endpoint пока оставлено. |
| A19 | Вынести HTTP-вызовы из транзакций (durable intent, cleanup, fencing) и замерить ожидание блокировок. Просто снять блокировки нельзя — ослабнут гарантии AI и оплат. |
| A20 | Воспроизводимый Python lock-файл с хешами и аудит уязвимостей именно собранного Linux-образа. |
| A21 | Поэтапно разделить большие сервисы grain/orders/cameras и крупные страницы фронта, общие DTO и валидаторы. |
| A23 | Как показывать удалённые финансовые документы. Смысл долга уже определён: непогашенный остаток отгруженного заказа (`orders/debt.py`). |
| A24 | Правила изменения уже активной поставки зерна и приоритет: строгая резервация или учёт фактически разгруженного при превышении плана. |
| A25 | Общее поколение бэкапа БД и media, устойчивая блокировка и проверка восстановления на отдельном стенде. |
| A07 | Суммы в UI идут через `Number`: для очень больших сумм нужен общий предел или decimal/тиыны на всех экранах. |

Эксплуатационные проверки, не сделанные в репозитории: нагрузка и планы запросов
на реальных данных, WebRTC/RTSP через Tailscale, точность весов и детектора,
восстановление из настоящего бэкапа.
